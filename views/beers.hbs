<div id="beers-main">
    {{#each beers}}
        <img src="{{this.image_url}}" alt="beer image">
        <h3>{{this.name}}</h3>
        <p>{{this.description}}</p>
        <p>{{this.tagline}}</p>
    {{/each}}
</div>